html标签分为两种单标签和双标签
单标签
<xx>
在传统的html版本中，单标签需要添加/，从h5开始但标签不再添加/


双标签
<xx></xx>
<xx> 开始标签     </xx>结束标签（闭合标签）


html标签按照内容可以分为三部分：标签名，属性，内容；
<xxx name="wz" age="11">content</xxx>

xxx 标签名
name、age 属性名   wz是name对应的值   11是age对应的值   多个属性中间需要用空格隔开
content是标签的内容

一个html标签可以没有属性，也可以没有内容，但必须要有标签名